# `policyengine_api.compute_api`

This module contains all the code for the API endpoints of the compute-intensive API. Long-running operations are carried out by the Compute API, run on a separate server.