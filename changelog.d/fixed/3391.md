* Add rerun support for legacy report outputs, including reset of linked simulations and economy cache rows.
* Pin `policyengine` below `1.0` so the legacy API continues to use the compatible `.py` interface.
