Migrate API v1 persistence to service-owned SQLAlchemy 2 sessions, direct mapped models, and Alembic while preserving public API contracts, including generated migrations that remove the orphaned `question` prototype table, keep tracer storage local-only, require reform-impact execution IDs, and require callers to provide the reform-impact dataset explicitly.
