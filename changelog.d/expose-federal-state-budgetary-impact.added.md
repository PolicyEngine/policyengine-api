Partition the `/economy` `budgetary_impact` payload into federal vs. state shares (`federal_budgetary_impact`, `state_budgetary_impact`, `federal_tax_revenue_impact`, `federal_benefit_spending_impact`, `state_benefit_spending_impact`) using `total_federal_benefit_cost` / `total_state_benefit_cost` keys from the economy worker (populated once policyengine-us exposes the FMAP-based attribution variables from PolicyEngine/policyengine-us#8076). Falls back to treating benefits as 100% federal when the worker hasn't populated the new keys (older releases).
